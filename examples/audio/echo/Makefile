# Echo / reverb — Family 8 (Audio), rung 8.9: the S-DSP echo unit

OPENSNES := $(shell cd ../../.. && pwd)

TARGET   := echo.sfc
ROM_NAME := ECHO REVERB

USE_LIB     := 1
LIB_MODULES := console audio input

CSRC   := main.c
ASMSRC := data.asm

include $(OPENSNES)/make/common.mk
