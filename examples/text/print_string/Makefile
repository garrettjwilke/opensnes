# Print a string — Family 1 (Text), rung 1.1: the built-in font

OPENSNES := $(shell cd ../../.. && pwd)

TARGET   := print_string.sfc
ROM_NAME := PRINT STRING

USE_LIB     := 1
LIB_MODULES := console dma text background sprite

CSRC   := main.c

include $(OPENSNES)/make/common.mk
