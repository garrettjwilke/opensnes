OPENSNES := $(shell cd ../.. && pwd)

TARGET   := benchrom.sfc
ROM_NAME := BENCHROM

USE_LIB  := 1
LIB_MODULES := console mode7 map

CSRC   := main.c
ASMSRC := data.asm


include $(OPENSNES)/make/common.mk
